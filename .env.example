# Client-exposed (safe to ship in the browser bundle - RLS is the real boundary)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Server-only (used by api/_lib/db.ts, never sent to the browser)
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
